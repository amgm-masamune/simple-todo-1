Deno.test("存在するタスクのIDを指定して削除できる。", async () => {
  throw "TODO";
});

Deno.test("対象以外のタスクは削除されない。", async () => {
  throw "TODO";
});

Deno.test("複数のタスクの中から削除できる。", async () => {
  throw "TODO";
});

Deno.test("存在しないタスクのIDを指定するとエラーになる。", async () => {
  throw "TODO";
});

Deno.test("削除済みのタスクのIDを指定するとエラーになる。", async () => {
  throw "TODO";
});

